import React from 'react';
import ReactDOM from 'react-dom';
import styled, { keyframes } from 'styled-components';
import AllBlobs from './AllBlobs';

class App extends React.Component {
    render() {
        return(<div><AllBlobs /></div>)
    }
}
export default App;